{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm run build",
    "watchPatterns": ["app/**", "components/**", "hooks/**", "lib/**", "services/**", "scripts/**", "public/**"]
  },
  "deploy": {
    "startCommand": "npm start",
    "healthcheckPath": "/"
  }
}
